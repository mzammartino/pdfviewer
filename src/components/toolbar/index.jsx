import React from 'react';
import './style.scss';

const Toolbar = () => (
  <div className="toolbar">
    <button type="button">
      Rotate left
    </button>
    <button type="button">
      Rotate right
    </button>
  </div>
);
export default Toolbar;
